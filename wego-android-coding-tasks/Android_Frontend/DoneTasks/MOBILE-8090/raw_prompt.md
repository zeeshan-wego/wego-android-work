# Create Separate Package for Staging Build

**Ticket:** MOBILE-8090

## Description

Right now we have single package for production and staging builds which create problem for QA to install both variant at once on single device. Task is to modify the code so that QA can install both prod and staging on same device.
